The python script SessionsGen.py generates a "well formed" Sessions.XML file
as output that can be imported by SuperPutty to generate a large number sessions
quickly. The input filename for the script will look for is the SiteList.txt textfile.