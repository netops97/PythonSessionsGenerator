The python script SessionsGen.py generates a "well formed" Sessions.XML file
as output that can be imported by SuperPutty to generate a large number of sessions
quickly. The input filename listing sites that need sessions is SiteList.txt text file.